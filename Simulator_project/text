Folder for project docs

Notizen: Ich habe deinen Circuit mit denen verglichen, welche ich von IBM angezeigt bekomme, nachdem der Job erledigt wurde; sie sind grundsätzlich identisch, lediglich verschachtelt
         dargestellt. Zur Kontrolle habe ich den QAOA_for_QLS und den QAOA2 nochmal an IBM geschickt und man sieht folgende Unterschiede:

         Der QAOA mit der Wrapper-Funktion (QAOA for QLS) braucht 4 jobs um zum Ergebnis zu gelangen und rotiert wirklich immer nur um sehr kleine Winkel.
         Die QAOA2 Implementierung mit dem qasm-Simulator braucht dagegen 37 Jobs (ein Versuch gemacht, beim 2. Versuch: 36) und rotiert um "normale" Winkel
         Sonst scheinen sich die Circuits von QAOA und QAOA2 aber nicht zu unterscheiden; einziger Unterschied: beim QAOA (Wrapper) findet bei den CNOT-Operationen wo Qubit1 als Target
         genommen wird noch eine RZ-Rotation am anderen Control-Qubit statt
         
         Ich habe auch noch eine Implementierung der Grover Adaptive Search commitet; die sollten wir aber vl über Skype besprechen (braucht 34 Iterationen und liefert falsches Ergebnis)
