Folder for project docs

Notizen: Der QAOA mit der Wrapper-Funktion (QAOA for QLS) braucht 4 jobs um zum Ergebnis zu gelangen und rotiert wirklich immer nur um sehr kleine Winkel
         Die QAOA2 Implementierung mit dem qasm-Simulator braucht dagegen 37 Jobs (ein Versuch gemacht) und rotiert um "normale" Winkel
         Sonst scheinen sich die Circuits von QAOA und QAOA2 aber nicht zu unterscheiden; einziger Unterschied: beim QAOA (Wrapper) findet bei der CNOT-Operationen wo Qubit1 als Control
         genommen wird noch eine RZ-Rotation am anderen Qubit statt
         
         Ich habe auch noch eine Implementierung der Grover Adaptive Search commitet; die sollten wir aber vl über Skype besprechen
